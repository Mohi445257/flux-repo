apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: wordpress
  namespace: default
spec:
  releaseName: wordpress
  chart:
    spec:
      chart: wordpress
      version: 15.2.33
      sourceRef:
        kind: HelmRepository
        name: bitnami
        namespace: flux-system
  interval: 1m
  values:
    wordpressUsername: user
    wordpressPassword: changeme
    wordpressEmail: user@example.com
    wordpressFirstName: User
    wordpressLastName: Example
    wordpressBlogName: "My Blog"

    mariadb:
      enabled: false   # disable the bundled MariaDB

    externalDatabase:
      host: mysql.default.svc.cluster.local
      user: wordpress
      password: ""              # leave empty → pulled from existingSecret
      database: wordpress
      existingSecret: mysql-secret   # must match your apps/mysql/secret-mysql.yaml

    ingress:
      enabled: true
      hostname: app.local
      ingressClassName: nginx
      tls: []  # configure later if you want HTTPS
