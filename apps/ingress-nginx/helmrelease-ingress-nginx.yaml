apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: ingress-nginx
  namespace: ingress-nginx
spec:
  releaseName: ingress-nginx
  interval: 5m
  chart:
    spec:
      chart: ingress-nginx
      version: 4.11.7
      sourceRef:
        kind: HelmRepository
        name: ingress-nginx
        namespace: flux-system
  values:
    controller:
      service:
        type: LoadBalancer
      config:
        # Allocate some memory for Lua shared dicts
        lua-shared-dicts: "plugins:10m"
        # Enable our custom Lua plugin (must match filename without .lua)
        plugins: "redirect_404"
      extraVolumes:
        - name: lua-plugins
          configMap:
            name: ingress-nginx-lua-plugins
      extraVolumeMounts:
        - name: lua-plugins
          mountPath: /etc/nginx/lua/plugins
          readOnly: true
